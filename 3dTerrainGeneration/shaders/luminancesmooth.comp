#version 430
layout(local_size_x = 1,local_size_y = 1,local_size_z = 1) in;
layout(std430, binding = 1) buffer outputData
{
    int maxLuma;
    float lumaSmooth;
};

void main() {
    lumaSmooth = (clamp(maxLuma / 1024., .5, 4.) + lumaSmooth * 19.) / 20.;
    maxLuma = 0;
}